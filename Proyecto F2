#include <iostream>
#include <string>
#include <iomanip>
int main(){
	int opcion;
	int minopcion;
	int cantcommits=0;
	std::string ultimoCommit;
	std::string nomrep;
	std::string nomaut;
	std::string nomarch;
	std::string commit;
	bool repositorioexiste = false;
	bool cambiospendientes = false;
do{

std::cout << "=========MINIGIT=========\n"<< std::endl;
std::cout << "1. Inicializar repositorio\n";
std::cout << "2. Ver informacion del repositorio\n";
std::cout << "3. Simular cambios\n";
std::cout << "4. Crear commit\n";
std::cout << "5. Ver ultimo commit\n";
std::cout << "6. Ver estado del repositorio\n";
std::cout << "7. Reiniciar repositorio\n";
std::cout << "8. Salir\n\n";

 while(!(std::cin >>opcion)){
 	
 	std::cin.clear();
 	std::cin.ignore(1000, '\n');
 	std::cout << "Digito invalido ingrese un numero\n";
 }
}
 //Primera Opcion Inicio//
 
 switch (opcion){
	case 1:
		 if (repositorioexiste==true){
    	 std::cout<< "\nYa existe un repositorio activo ";
    	 std::cout<< "\nRegresando al menu principal\n\n";
	     break;
         }
		 std::cin.ignore();
		 do{
		 std::cout<< "\nDigite el nombre del repositorio\n";
	     std::getline(std::cin, nomrep);
	     if (nomrep.empty()) {
            std::cout << "\nEl nombre del repositorio no puede quedar vacio. Intente de nuevo.\n";
	   	 } }while(nomrep.empty()); 
         do{
 	
	     std::cout<< "\nDigite el nombre del autor\n";
	     std::getline(std::cin, nomaut);
	     if (nomaut.empty()) {
         std::cout << "/nEl nombre del autor no puede quedar vacio. Intente de nuevo.\n";
	     } 
         }while (nomaut.empty());
       
         repositorioexiste = true;
      
         std::cout << "\nRepositorio " << nomrep << " creado exitosamente por " << nomaut << "\n\n";
         break; 
     
     // Primera Opcion Fin
     
     // Segunda Opcion Inicio
     
     case 2:
     	if (repositorioexiste== false){
     		std::cout << "\n---------"<<"\nNo existe un repositorio en este momento";
     		std::cout << "\nCree un Repositorio para ver la informacion\n"<<"---------\n\n";
     		break;} 
		 std:: cout << "\n---------" << "\nRepositorio: " << nomrep<< "\n";
		 std:: cout << "Autor: "<<nomaut<<"\n";
		 std:: cout << "Estado: Inicializado" << "\n";                //Preguntar como exactamente hacer el estado inicializado y los cambios pendientes//
		 std:: cout << "Cantidad de Commit: " << cantcommits << "\n";
		 std:: cout << "Ultimo Commit: " << "#" << cantcommits "\n";
		 if (cambiospendientes == true){
		 
		     std:: cout << "Cambios Pendientes: " << "Si" << "\n" << "---------\n\n";
		 }else{
		 	std:: cout << "Cambios Pendientes: " << "No" << "\n" << "---------\n\n";
		 }
		 break; 
		  //Segunda Opcion Fin (Faltan Cambios)//
		 
	  //Tercera Opcion Inicio	 
	 case 3:
	 	if (repositorioexiste== false){
     		std::cout << "\n---------"<<"\nNo existe un repositorio en este momento";
     		std::cout << "\nCree un Repositorio para simular cambios\n"<<"---------\n\n";
     		break;}
	 	std:: cout <<"\n==Simulacion de Cambios==";
	 	 std::cout << "\n\n1. Agregar Archivo\n";
         std::cout << "2. Modificar Arichivo\n";
         std::cout << "3. Eliminar Archivo\n";
         std::cout << "4. Cancelar\n\n";
		 std:: cin >> minopcion;   
		 
	 	switch (minopcion){
	 		case 1:
	 		case 2:
	 		case 3:
	 			std::cout << "\nPorfavor ingrese el nombre de el archivo\n\n";
	 			std::cin >> nomarch;
	 			cambiospendientes = true;
	 			std::cout << "\nModificacion de el archivo realizada\n\n";
	 			break;
	 		case 4:
	 			 std::cout << "\nRegresando al menu principal\n\n";
                break;
                
            default:
                std::cout << "\nOpcion invalida. Intente de nuevo.\n";
	 			
		 } break;
     //Tercera Parte Fin//
      
     //Cuarta Parte Inicio//
    case 4:
         
         if (repositorioexiste == false) {
             std::cout << "\n-------------------------------------------------\n";
             std::cout << "Debe inicializar un repositorio antes de crear commits.\n";
             std::cout << "-------------------------------------------------\n\n";
             break;
         } 
         
        
         if (cambiospendientes == false) {
             std::cout << "\n-------------------------------------------------\n";
             std::cout << "No existen cambios para guardar.\n";
             std::cout << "-------------------------------------------------\n\n";
             break;
         }
         
         
         std::cin.ignore();
         
       
         do {
             std::cout << "\nIngrese el mensaje del commit (10 a 60 caracteres):\n";
             std::getline(std::cin, ultimoCommit);

             if (ultimoCommit.length() < 10 || ultimoCommit.length() > 60) {
                 std::cout << "El mensaje debe contener entre 10 y 60 caracteres. Intente de nuevo.\n";
             }
         } while (ultimoCommit.length() < 10 || ultimoCommit.length() > 60);

        
         cantcommits++;
         cambiospendientes = false;

         std::cout << "\nCommit #" << cantcommits << " creado correctamente.\n\n";
         break;
   //Cuarto caso fin//

//Quinto caso Inicio//
case 5:
	if (cantcommits == 0){
		std::cout <<"No existe un commit en este momento cree uno para ver la informacion\n\n";
		break;
	}else{
	
	std::cout << "Commit #" << cantcommits;
	std::cout << "\n\nRepositorio:\n"<<nomrep;
	std::cout <<"\n\nAutor:\n"<<nomaut;
	std::cout <<"\n\nMensaje:\n"<<"Se agregaron validaciones al menú.";
	std::cout <<"\n\nEstado:\n"<<"Guardado correctamente\n\n";
	break;
	}
	//Quinto caso Fin//
	
	//Sexto Caso Inicio//
case 6:
	 if (repositorioexiste == false){
        std::cout << "\n---------"<<"\nNo existe un repositorio en este momento";
        std::cout << "\nCree un Repositorio para ver su estado\n"<<"---------\n\n";
        break;
    }

    {  
        std::string estado = "Inicializado | ";
        estado += cambiospendientes ? "Cambios pendientes" : "Sin cambios pendientes";

      

        std::cout << "\n";
        std::cout << std::left << std::setw(15) << "Repositorio"
                   << std::setw(35) << "Estado"
                   << "Ultimo commit" << "\n";
        std::cout << std::string(65, '-') << "\n";
        std::cout << std::left << std::setw(15) << nomrep
                   << std::setw(35) << estado << "     #"
                   << cantcommits << "\n\n";
    }
    break;
    //Sexto caso Fin//
    // Septimo Caso Inicio
    case 7:
        if (!repositorioexiste) {
            std::cout << "\n-------------------------------------------------\n";
            std::cout << "No existe un repositorio para reiniciar.\n";
            std::cout << "-------------------------------------------------\n\n";
            break;
        }

        int opcionReiniciar;
        std::cout << "\n¿Desea eliminar toda la informacion?\n";
        std::cout << "1. Si\n";
        std::cout << "2. No\n";
        std::cout << "Seleccione una opción: ";

        while (!(std::cin >> opcionReiniciar) || (opcionReiniciar != 1 && opcionReiniciar != 2)) {
            std::cin.clear();
            std::cin.ignore(1000, '\n');
            std::cout << "Opcion invalida. Ingrese 1 para Si o 2 para No: ";
        }

        if (opcionReiniciar == 1) {
            
            repositorioexiste = false;
            cambiospendientes = false;
            cantcommits = 0;
            nomrep = "";
            nomaut = "";
            nomarch = "";
            ultimoCommit = "";

            std::cout << "\nEl repositorio ha sido reiniciado exitosamente.\n\n";
        } else {
            std::cout << "\nOperación cancelada. Regresando al menú principal.\n\n";
        }
        break;
    // Septimo Caso Fin
    
    //Caso Ocho inicio//
    case 8:
    	break;
    // Caso Ocho Fin//
    default:
    	std::cout << "Numero invalido ingrese uno el cual este en el menu";
    	break;
	}

	

//Cuarta Parte Fin (En proceso)//
 
		 


}while (opcion!= 8);
std::cout << "\n\nProceso finalizado tenga un lindo dia";
return 0;
	
}
